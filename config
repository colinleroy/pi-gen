IMG_NAME=surl-server
TARGET_HOSTNAME=surl-server
ENABLE_SSH=1
FIRST_USER_NAME=pi
FIRST_USER_PASS=raspberry
DISABLE_FIRST_BOOT_USER_RENAME=1
RELEASE=bullseye
BASE_QCOW2_SIZE=4G

LOCALE_DEFAULT=en_US.UTF-8

IMG_DATE="$(date +%Y-%m-%d)"
IMG_FILENAME="${IMG_NAME}-${RELEASE}-${IMG_DATE}"
ARCHIVE_FILENAME="${IMG_NAME}-${RELEASE}-${IMG_DATE}"
DEPLOY_COMPRESSION=gz
